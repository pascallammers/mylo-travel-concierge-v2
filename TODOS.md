# TODOs

Generated by /plan-eng-review on 2026-04-27 for `feature/flightdeals-apify`.

Status legend: `[OPEN]` not started · `[WIP]` in progress · `[DONE]` shipped · `[BLOCKED]` waiting

---

## 1. [OPEN] `tool_calls` Tabelle Tracking-Bug debuggen

**What:** Investigate why `tool_calls` table has 0 rows in 30 days while `message.parts` shows 762+ tool invocations. Either fix tracking or deprecate the table.

**Why:** KPI dashboard depends on per-tool latency/error data. Phase-Gate measurements fall back to `message.parts` JSON-filtering today — works but fragile (parts schema can change). Forensic debugging of failed tool runs is impossible without `tool_calls`.

**Pros:** Granular status/latency/error logs, durable Phase-Gate source, KPI dashboard accuracy.

**Cons:** ~½–1 day debug time without immediate Phase-1 ROI.

**Context:** Schema at `lib/db/schema.ts:358`. Insert call site needs to be traced (likely in `app/api/search/route.ts` tool-execution path or AI SDK middleware). 30-day query: `SELECT COUNT(*) FROM tool_calls WHERE created_at >= NOW() - INTERVAL '30 days'` returns 0.

**Depends on / blocked by:** Nothing. Standalone fix, schedule any time post-Phase-1.

---

## 2. [OPEN] AwardWallet kommerzielle T&C + Token-Refresh-Resilience prüfen

**What:** Verify (a) AwardWallet Business API explicitly permits commercial SaaS use including resale of insights, (b) token refresh works across multi-day idle periods, (c) DSGVO data-processing agreement is in place, (d) user consent flow at `/settings/loyalty` connection is unambiguous about what data MYLO stores.

**Why:** Codex outside-voice flagged this as a hidden risk. User-Decision: consent is implied through user-initiated AwardWallet connection. But token refresh, T&C compliance, and DPA are operational risks not addressed.

**Pros:** Removes a legal blast-radius risk before scaling Phase 1 to all 217 paying users.

**Cons:** ~1 day operational work (read T&C, contact AwardWallet support, update privacy policy, test token refresh).

**Context:** `lib/api/awardwallet-client.ts` (301 LoC). Connection flow lives at `/settings/loyalty`. Schema: `awardwalletConnections` table at `lib/db/schema.ts:633`. Current behavior: `loyalty-prompt-formatter.ts` injects loyalty data into AI system context.

**Depends on / blocked by:** Nothing. Should be resolved before Phase 1 ships to production but is not a code-time blocker.

---

## 3. [OPEN] KB-Content für Award-Domain erweitern

**What:** Expand the knowledge base with curated Award-Optimization content (M&M CPP bands, Avios sweet spots for German market, AmEx MR transfer ratios, common Lufthansa Allegris routes). Goal: KB stops returning `__KB_ERROR__` for "Lohnt sich X€ für Y Meilen?"-class questions.

**Why:** Phase-1 cpp_calculator solves the deterministic math case. KB still degrades gracefully to web_search for general Award questions. But upstream KB content for the Award domain would reduce web_search latency (currently 5+ queries per ambiguous Award question) and lock in MYLO's domain authority.

**Pros:** Faster Award answers, fewer web_search calls (cost reduction), better SEO of MYLO outputs vs ChatGPT (proprietary content moat).

**Cons:** Content authoring is partner-dependent (Single-Point-of-Failure risk noted in plan). Effort: ~3–5 days curated content writing per domain area.

**Context:** Knowledge base lives in `lib/tools/knowledge-base.ts` + `lib/tools/knowledge-base-query.ts`. KB documents are managed via `kb_documents` schema and admin UI. Current observed gap: M&M-CPP queries return `__KB_ERROR__`, AI falls to web_search.

**Depends on / blocked by:** Phase 1 ship (so we know which questions still hit KB-fallback after cpp_calculator is live). Partner-content delivery for sweet spots.

---

## 4. [OPEN] Cross-Check "Trust Travelpayouts >15%"-Policy datenbasiert validieren

**What:** After Phase 2 ships, audit `deal_source_disagreements` log entries for 4 weeks. Determine: is Travelpayouts systematically higher (cache lag) or systematically lower (Ignav scrapes hidden inventory)? Adjust trust-policy based on evidence rather than the current 15%-threshold heuristic.

**Why:** Codex flagged this: "Bei Flight Deals ist der günstigere Preis oft der einzige Grund zur Conversion. Falsche Trust-Policy kann die besten Deals wegfiltern." 15% is a guess from the design doc, not data-driven.

**Pros:** Better deal-quality on `/deals` page, fewer false-positives filtered out, higher conversion potential.

**Cons:** Requires 4 weeks of disagreement data first. Until then, current heuristic is operational.

**Context:** Phase 2 logs disagreements per design. Suggested: introduce dedicated `deal_source_disagreements` table (Section-2 finding C3) instead of misusing `failed_search_logs`. Audit query: avg/median disagreement size, conversion rate of trusted-source vs ignored-source deals.

**Depends on / blocked by:** Phase 2 ship + 4 weeks runtime.

---

## 5. [OPEN] Apify-Branch-Name umbenennen oder dokumentieren

**What:** Decide whether `feature/flightdeals-apify` should be renamed to `feature/two-tier-stack` (or merged-and-archived) at Phase-1-merge. If kept, document why in repo (e.g., "Apify reserved for Google-Flights-fallback in Phase 4").

**Why:** Plan Open Question 4. Branch name no longer reflects strategy. Codex also flagged: "Manchmal überleben Branch-Namen länger als die Strategien hinter ihnen."

**Pros:** Cleaner Git history, lower onboarding friction for future contributors.

**Cons:** Negligible. 5-min decision.

**Context:** Branch at HEAD `ab0c14a`. Last 5 commits unrelated to Apify (subscriptions, deal-gating, AI providers, admin filters).

**Depends on / blocked by:** Phase-1-merge timing.

---

## 6. [OPEN] Cancel-Rate-Baseline SQL pre-Phase-1

**What:** Run SQL against `subscription` table: cancel-rate per month for Travel-Concierge-Plan, excluding 0€-trials. Persist as Phase-1 success-metric comparison value.

**Why:** Plan Assignment item 0 explicitly required. Without baseline, the "-30% in 60 days" success criterion is unmeasurable. Reviewer Concern 4: "-30% in 60 Tagen ist Stretch-Goal" — even more reason to know the starting line.

**Pros:** Phase-1 retention claim becomes verifiable. Honest signal vs noise.

**Cons:** None. ~10 min work.

**Context:** Schema: `subscription` table at `lib/db/schema.ts:112`, `archive_subscription` for canceled. Filter: `planType` matches Travel-Concierge product, `customerCancellationReason` populated, exclude 0€-trial wave Feb-March 2026 explicitly.

**Depends on / blocked by:** Must be done BEFORE Phase-1 ships. Day-0 task in week 1.

---

## 7. [OPEN] Power-User-Verteilung re-messen nach Phase 1

**What:** Re-run the SQL from `/plan-eng-review` outside-voice resolution: `unique_users_with_award_intent / active_users` for the 30 days following Phase-1 ship. Compare to the pre-Phase-1 baseline (33 unique award-users out of 63 active = 52.4%).

**Why:** Codex flagged the 28%-message-based metric as Power-User-skewed. We measured 52.4% of active users have Award intent. Phase 1's success premise rests on this — if post-ship the ratio drops or Power-User concentration grows, the tools are reaching the wrong audience and Phase-2 strategy needs reconsideration.

**Pros:** Phase-1 → Phase-2 transition is data-driven, not gut-driven. Signals false-positive case where Award tools only delight existing Power-Users.

**Cons:** Requires Phase-1-shipping first.

**Context:** Query already prepared and tested. See `/plan-eng-review` session output. Save query in `lib/db/queries/award-users.sql` for repeated runs.

**Depends on / blocked by:** Phase-1 ship + 30 days runtime.

---

## 8. [OPEN] DESIGN.md schreiben (leichtgewichtig)

**What:** Author a lightweight DESIGN.md at the repo root documenting color tokens (CSS variables in `app/globals.css`), type-scale, spacing, the alert-variant system (info/warning/success/error), and a components-inventory. Not a full design system — just enough that any implementer (or future Pascal) knows the rules without reverse-engineering existing components.

**Why:** Plan-Design-Review surfaced the gap. With Phase 2 SEO-Detail-Pages, Bucket-Showcase, Onboarding-Banner, Empty-States — multiple new UI components ship in parallel. Without a DESIGN.md, drift between the new pages and existing deal-card-style is guaranteed. A 200-line DESIGN.md prevents that.

**Pros:** Single source-of-truth for design decisions. Reduces implementer questions. Makes future design review faster (calibrate against stated rules).

**Cons:** ~3-4 hours to author. Maintenance burden as design evolves.

**Context:** shadcn/ui style="new-york", baseColor="neutral", CSS variables already in `app/globals.css`. Lucide icons. 45 ui/ components, 6 deal/ components. Document what's there, don't invent new rules.

**Depends on / blocked by:** Nothing. Should be done before Phase 2 ships, ideally before SEO-Detail-Page implementation starts.

---

## 9. [OPEN] Banner/Banner-Adoption-Analytics einbauen

**What:** Track AwardWallet-Banner views, dismiss-clicks, connect-clicks, and successful connections. Goal: measure adoption-rate as a Phase-1-Gate input.

**Why:** Plan-Doc Reviewer Concern 3: "AwardWallet-Adoption-Rate unbekannt — wenn weniger als 10% der 217 Subs ihren AwardWallet-Account verbunden haben, wird die manuelle Eingabe der Default-Pfad". Without analytics, you can't tell if the smart-trigger banner actually drives connections or if users dismiss it.

**Pros:** Concrete adoption-rate data. Iterate on Banner-Copy and Trigger-Logic if numbers are weak. Phase-1-Gate metric becomes verifiable.

**Cons:** ~2-3h to wire up event-tracking (likely via existing analytics-tracking infrastructure or Vercel Analytics).

**Context:** Banner trigger logic in `components/awardwallet/` (likely needs new component). Analytics integration pattern: check existing `lib/utils/loyalty-prompt-formatter.ts` for how loyalty-events are surfaced. Vercel Analytics + custom events likely the lowest-friction path.

**Depends on / blocked by:** Phase 1 banner ships first.

---

## 10. [OPEN] Alert-Variant-Tone-Map dokumentieren

**What:** Document the 4 alert variants (info/warning/success/error) in DESIGN.md (or a dedicated `components/ui/alert.md`) with tone, when-to-use guidelines, and example copy per variant.

**Why:** Plan-Design-Review identified Banner-Hell-Risk: Onboarding-Banner (info) + Stale-Banner (warning) + Connection-Success (success) + OAuth-Fail (error). Without a documented tone-map, future implementers will mix amber for stale and amber for caution and red for "stop" — drift accumulates.

**Pros:** Prevents alert-style drift. Faster design review for future PRs touching banners. Makes shadcn Alert variant choices automatic, not negotiated each time.

**Cons:** Trivial — ~30 min to write.

**Context:** Existing `components/ui/alert.tsx` is shadcn default. Variants need explicit guidelines: info=blau-subtle (passive notification), warning=amber-subtle (Aufmerksamkeit + Aktion empfohlen, nicht dismissible), success=green-subtle (Bestätigung, auto-hide nach 4s), error=red-subtle (Failure + Retry-Path).

**Depends on / blocked by:** TODO 8 (DESIGN.md). Best done in same edit.

---

## 11. [OPEN] Apify Last-Resort-Fallback für Sites ohne API

**What:** Plan-Doc mentions Apify as Last-Resort-Fallback for sites without API (Google Flights). Decide: scrap entirely (per Solo-Founder bandwidth), or formalize as Phase 4+ option. If formalized, document scope and trigger conditions ("if Travelpayouts coverage drops below X%").

**Why:** Branch name suggests Apify was the original strategy. Current plan removes it. But "Sites ohne API" is a real long-tail problem (e.g., niche European charter airlines on Google Flights only). Decide explicitly rather than letting the branch-name imply an undelivered feature.

**Pros:** Clear position on whether Apify ever returns. Avoids zombie-feature-flag in repo.

**Cons:** Forces a decision today. Might be premature.

**Context:** No Apify integration exists in code today. Decision is purely scope/strategy.

**Depends on / blocked by:** Phase-Gate 3 outcome. If DACH unit-economics are healthy and Travelpayouts coverage is sufficient, scrap. If Phase-3 reveals coverage gaps, revisit.
